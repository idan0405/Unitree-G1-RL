# G1 locomotion (MuJoCo + Stable-Baselines3 PPO)
# Python 3.10+.  Stable-Baselines3 pulls in PyTorch automatically;
# install the CUDA build of torch first if you want GPU training.
gymnasium>=0.29
mujoco>=3.1
numpy>=1.24
stable-baselines3>=2.3
tensorboard>=2.14
